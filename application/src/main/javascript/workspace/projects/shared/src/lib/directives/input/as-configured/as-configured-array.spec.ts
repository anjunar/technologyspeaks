import { AsConfiguredArray } from './as-configured-array';

describe('AsConfiguredArray', () => {
  it('should create an instance', () => {
    const directive = new AsConfiguredArray();
    expect(directive).toBeTruthy();
  });
});
