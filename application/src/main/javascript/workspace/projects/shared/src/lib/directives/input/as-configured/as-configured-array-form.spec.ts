import { AsConfiguredArrayForm } from './as-configured-array-form';

describe('AsConfiguredArrayForm', () => {
  it('should create an instance', () => {
    const directive = new AsConfiguredArrayForm();
    expect(directive).toBeTruthy();
  });
});
