import { AsConfiguredForm } from './as-configured-form';

describe('AsConfiguredForm', () => {
  it('should create an instance', () => {
    const directive = new AsConfiguredForm();
    expect(directive).toBeTruthy();
  });
});
