import { AsConfiguredLazySelect } from './as-configured-lazy-select';

describe('AsConfiguredLazySelect', () => {
  it('should create an instance', () => {
    const directive = new AsConfiguredLazySelect();
    expect(directive).toBeTruthy();
  });
});
