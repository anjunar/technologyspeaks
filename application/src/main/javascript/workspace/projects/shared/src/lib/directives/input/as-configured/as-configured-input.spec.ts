import { AsConfiguredInput } from './as-configured-input';

describe('AsConfiguredInput', () => {
  it('should create an instance', () => {
    const directive = new AsConfiguredInput();
    expect(directive).toBeTruthy();
  });
});
