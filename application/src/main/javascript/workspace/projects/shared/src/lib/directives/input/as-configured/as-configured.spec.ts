import AsConfigured from './as-configured';

describe('AsConfigured', () => {
  it('should create an instance', () => {
    const directive = new AsConfigured();
    expect(directive).toBeTruthy();
  });
});
